# Optional. Only needed if this repo is deployed as a Cloudflare Pages project.
# The name must match the Pages project name exactly.
name = "halyard"
compatibility_date = "2026-07-18"

[assets]
directory = "./"
